/**
 * Punto de entrada del entorno: por defecto apunta a **local**.
 * En build, Angular sustituye este archivo según la configuración (ver `angular.json`).
 */
export { environment } from './environment.local';
export type { AppEnvironment, AppEnvironmentName } from './environment.interface';
